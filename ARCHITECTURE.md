# Multi-Agent 商业地产投资分析系统 - 架构设计文档

## 1. 项目概述

本项目是一个基于 **LangChain** 实现的智能商业地产投资分析系统，融合了多Agent编排、RAG知识增强、LoRA领域微调等核心技术能力，为高净值用户提供专业的商业地产投资决策支持服务。

**核心业务场景：**
- 商业地产市场分析与趋势预测
- 投资收益测算与可行性评估
- 风险合规审查与政策解读

**核心技术亮点：**
- **Multi-Agent 协作**：主Agent + 3垂类投资分析Agent（市场分析、收益测算、风险合规）的智能编排
- **RAG知识增强**：Milvus向量库 + 多路召回 + RRF融合排序，投资方案生成准确率 65%→91%
- **LoRA微调**：千问7B模型商业地产领域适配，准确率 72%→88%
- **高可用架构**：FastAPI + Nginx + Sentinel保障高并发，支持1000+高净值用户

---

## 2. 整体架构

### 2.1 架构分层

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        接入层 (Access Layer)                            │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                          │
│  │ 企业微信 │    │    OA    │    │    H5    │                          │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘                          │
│       │               │               │                                 │
└───────┼───────────────┼───────────────┼────────────────────────────────┘
        │               │               │
        ▼               ▼               ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        网关层 (Gateway Layer)                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Nginx 负载均衡                                │   │
│  │  反向代理  │  负载均衡  │  健康检查  │  SSL/TLS终止              │   │
│  └───────────────────────┬──────────────────────────────────────────┘   │
│                          │                                              │
│  ┌───────────────────────▼──────────────────────────────────────────┐   │
│  │                   Sentinel 熔断限流                               │   │
│  │  流量控制  │  熔断降级  │  系统保护  │  热点隔离                  │   │
│  └───────────────────────┬──────────────────────────────────────────┘   │
└───────────────────────────┼──────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        应用层 (Application Layer)                       │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    FastAPI 服务网关                               │   │
│  │  RESTful API  │  请求路由  │  参数校验  │  响应封装               │   │
│  └───────────────────────┬──────────────────────────────────────────┘   │
│                          │                                              │
│  ┌───────────────────────▼──────────────────────────────────────────┐   │
│  │                  Multi-Agent 编排引擎                             │   │
│  │  ┌────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    │   │
│  │  │ 主Agent│───▶│市场分析  │    │收益测算  │    │风险合规  │    │   │
│  │  │ (调度) │    │ Agent    │    │ Agent    │    │ Agent    │    │   │
│  │  └────────┘    └──────────┘    └──────────┘    └──────────┘    │   │
│  │  Agent选择 │  任务分发  │  结果聚合  │  工具调用                   │   │
│  └───────────────────────┬──────────────────────────────────────────┘   │
│                          │                                              │
│  ┌───────────────────────▼──────────────────────────────────────────┐   │
│  │                    会话状态管理                                   │   │
│  │              ┌───────────────────────┐                            │   │
│  │              │      Redis 缓存层     │                            │   │
│  │              │ 会话存储│历史管理│过期策略                          │   │
│  │              └───────────────────────┘                            │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      数据/服务层 (Data/Service Layer)                   │
│                                                                       │
│  ┌───────────────────────┐          ┌───────────────────────┐          │
│  │        RAG模块        │          │         LLM模块        │          │
│  │  ┌─────────────────┐  │          │   ┌─────────────────┐  │          │
│  │  │   Milvus向量库   │  │          │   │ 千问7B(LoRA微调) │  │          │
│  │  │ 语义检索│多路召回 │  │          │   │ 方案生成│响应优化 │  │          │
│  │  └────────┬────────┘  │          │   └─────────────────┘  │          │
│  │           │           │          │                        │          │
│  │  ┌────────▼────────┐  │          │                        │          │
│  │  │   投资知识库     │  │          │                        │          │
│  │  │    (2万+文档)    │  │          │                        │          │
│  │  └─────────────────┘  │          │                        │          │
│  └───────────────────────┘          └───────────────────────┘          │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                        工具服务层                                 │   │
│  │ 地产行情API │ 政策数据库 │ 地块数据库 │ 租金测算工具 │ 风险评估模型 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件说明

| 组件 | 职责 | 技术栈 |
|------|------|--------|
| **主Agent** | 接收请求、分析意图、选择垂类Agent、聚合结果 | LangChain AgentExecutor |
| **市场分析Agent** | 调取地产行情、政策、地块数据，进行市场趋势分析 | LangChain + 地产数据工具 |
| **收益测算Agent** | 计算租金收益、回报周期、收益率，评估投资可行性 | LangChain + 财务测算工具 |
| **风险合规Agent** | 政策校验、风险提示、合规审查，识别潜在风险 | LangChain + 合规工具 |
| **RAG模块** | 知识库检索、语义匹配、多路召回融合 | Milvus + LangChain Retriever |
| **LLM模块** | 对话理解、投资方案生成、响应优化 | 千问7B + LoRA |
| **会话管理** | 多轮对话上下文维护、状态缓存 | Redis |

### 2.3 垂类Agent详细职责

#### 垂类Agent1：市场分析Agent
| 功能 | 说明 | 调用工具 |
|------|------|----------|
| 地产行情分析 | 获取商业地产市场动态、价格走势 | 地产行情API |
| 政策解读 | 分析最新房地产政策影响 | 政策数据库 |
| 地块数据查询 | 获取目标地块信息（位置、面积、规划用途） | 地块数据库 |
| 市场趋势预测 | 基于历史数据预测市场走向 | 趋势分析模型 |

#### 垂类Agent2：收益测算Agent
| 功能 | 说明 | 调用工具 |
|------|------|----------|
| 租金收益计算 | 基于市场数据估算租金收入 | 租金测算工具 |
| 回报周期分析 | 计算投资回收期 | 财务模型 |
| 收益率评估 | 计算IRR、NPV等关键指标 | 财务计算器 |
| 可行性评估 | 综合评估投资项目可行性 | 可行性分析引擎 |

#### 垂类Agent3：风险合规Agent
| 功能 | 说明 | 调用工具 |
|------|------|----------|
| 政策合规校验 | 检查项目是否符合政策要求 | 合规审查引擎 |
| 风险识别 | 识别投资风险点（政策风险、市场风险等） | 风险评估模型 |
| 风险提示 | 输出风险等级和风险说明 | 风险报告生成器 |
| 合规建议 | 提供合规优化建议 | 合规知识库 |

---

## 3. 核心业务流程

### 3.1 用户请求处理流程

```
用户提问
    ↓
┌─────────────────┐
│  Nginx 负载均衡 │  ← 分配请求到可用的API服务节点
└────────┬────────┘
         ↓
┌─────────────────┐
│ Sentinel限流检查│  ← 超过限流阈值则拒绝请求
└────────┬────────┘
         ↓ (通过)
┌─────────────────┐
│ FastAPI网关     │  ← 接收请求，参数校验
└────────┬────────┘
         ↓
┌─────────────────┐
│ Redis会话管理   │  ← 检查会话是否存在
└────────┬────────┘
         ↓ (存在/创建)
┌─────────────────┐
│ 主Agent调度     │  ← 分析用户意图
└────────┬────────┘
         ↓
┌─────────────────┐
│ 选择垂类Agent   │  ← 市场分析/收益测算/风险合规
└────────┬────────┘
         ↓
┌─────────────────┐
│ 工具调用+RAG检索│  ← 获取数据和知识
└────────┬────────┘
         ↓
┌─────────────────┐
│ 千问7B生成方案  │  ← 整合分析结果
└────────┬────────┘
         ↓
┌─────────────────┐
│ 更新Redis会话   │  ← 保存对话历史
└────────┬────────┘
         ↓
    返回结果给用户
```

**流程说明：**
1. **用户提问**：用户通过企业微信/OA/H5发起投资咨询
2. **负载均衡**：Nginx将请求分发到后端API节点
3. **限流检查**：Sentinel判断是否超过流量阈值
4. **API网关**：FastAPI接收请求并进行参数校验
5. **会话管理**：从Redis加载或创建会话上下文
6. **意图识别**：主Agent分析用户需求类型
7. **Agent调用**：选择对应的垂类Agent执行分析
8. **数据获取**：调用外部工具和RAG知识库检索
9. **方案生成**：千问7B整合信息生成投资建议
10. **结果返回**：保存会话并返回分析报告

---

### 3.2 Agent协作流程

```
用户: "分析上海陆家嘴商业地块投资价值"
         ↓
┌─────────────────────┐
│   主Agent意图识别   │
└──────────┬──────────┘
           ↓
    综合分析需求
           ↓
   ┌───────┴───────┐
   ↓               ↓               ↓
┌─────────┐   ┌─────────┐   ┌─────────┐
│市场分析 │   │收益测算 │   │风险合规 │
│ Agent   │   │ Agent   │   │ Agent   │
└────┬────┘   └────┬────┘   └────┬────┘
     ↓             ↓             ↓
┌─────────┐   ┌─────────┐   ┌─────────┐
│调取行情 │   │租金测算 │   │合规审查 │
│政策解读 │   │IRR/NPV │   │风险识别 │
└────┬────┘   └────┬────┘   └────┬────┘
     └───────┬───────┴───────┬───────┘
             ↓               ↓
        ┌─────────────────────────┐
        │      主Agent汇总        │
        └─────────────┬───────────┘
                      ↓
            ┌─────────────────┐
            │  千问7B整合生成  │
            └────────┬────────┘
                     ↓
            综合投资分析报告 → 返回给用户
```

**Agent职责分工：**

| Agent | 职责 | 输出 |
|-------|------|------|
| **市场分析Agent** | 调取地产行情、政策、地块数据 | 市场分析报告 |
| **收益测算Agent** | 计算租金收益、回报周期、IRR/NPV | 收益测算报告 |
| **风险合规Agent** | 政策合规校验、风险识别 | 风险合规报告 |
| **主Agent** | 意图识别、任务分发、结果聚合 | 综合分析报告 |

---

### 3.3 RAG知识检索流程

```
用户问题
    ↓
┌─────────────────┐
│  向量化编码     │  ← 将问题转换为向量表示
└────────┬────────┘
         ↓
┌─────────────────┐
│   多路召回策略   │
└────────┬────────┘
         ↓
┌───────┬───────────┬───────────┐
│ 向量  │  关键词   │   语义    │
│ 检索  │   匹配    │   匹配    │
└───┬───┴─────┬─────┴─────┬─────┘
    │         │           │
    └─────────┼───────────┘
              ↓
    ┌─────────────────┐
    │  候选文档集     │  ← 合并去重
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  RRF融合排序    │  ← 按相关性排序
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │   Top-K筛选     │  ← 取前10条最相关文档
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │  构建Prompt上下文│ ← 将文档填入提示词模板
    └────────┬────────┘
             ↓
    ┌─────────────────┐
    │   千问7B推理    │  ← 生成最终回答
    └────────┬────────┘
             ↓
        返回给用户
```

**三路召回说明：**

| 召回方式 | 原理 | 特点 |
|----------|------|------|
| **Milvus向量检索** | 基于向量相似度匹配 | 语义理解能力强 |
| **BM25关键词匹配** | 基于TF-IDF算法 | 关键词匹配准确 |
| **语义相似度匹配** | 计算句子级相似度 | 上下文理解好 |

**RRF融合排序：** 将三种召回结果按排名融合，取倒数求和作为最终分数，有效整合不同召回策略的优势。

---

## 4. 项目目录结构

```
AI investment agent/
├── api/                    # API接口层
│   ├── __init__.py
│   ├── v1/                 # API版本控制
│   │   ├── __init__.py
│   │   ├── endpoints/      # 接口端点
│   │   │   ├── chat.py     # 对话接口
│   │   │   ├── investment.py # 投资分析接口
│   │   │   └── health.py   # 健康检查
│   │   └── schemas/        # 请求/响应模型
│   │       ├── request.py
│   │       └── response.py
│   └── dependencies.py     # 依赖注入
├── agent/                  # Agent模块
│   ├── __init__.py
│   ├── main_agent.py       # 主Agent（调度中心）
│   ├── market_agent.py     # 市场分析Agent
│   ├── revenue_agent.py    # 收益测算Agent
│   ├── risk_agent.py       # 风险合规Agent
│   └── tools/              # 工具集
│       ├── market_tools.py # 地产行情/政策/地块工具
│       ├── revenue_tools.py # 租金测算/财务计算工具
│       ├── risk_tools.py   # 合规审查/风险评估工具
│       └── rag_tools.py    # RAG检索工具
├── rag/                    # RAG知识增强模块
│   ├── __init__.py
│   ├── vector_store.py     # Milvus向量库操作
│   ├── retriever.py        # 多路召回检索器
│   ├── rrf_reranker.py     # RRF融合排序
│   └── knowledge_base.py   # 知识库管理
├── llm/                    # LLM推理模块
│   ├── __init__.py
│   ├── model.py            # 千问7B模型封装
│   ├── lora_adapter.py     # LoRA适配器
│   └── prompt_template.py  # 商业地产领域提示词模板
├── session/                # 会话管理模块
│   ├── __init__.py
│   ├── redis_client.py     # Redis客户端
│   ├── session_manager.py  # 会话状态管理
│   └── context_store.py    # 上下文存储
├── config/                 # 配置模块
│   ├── __init__.py
│   ├── settings.py         # 应用配置（环境变量）
│   ├── agent_config.py     # Agent配置
│   ├── milvus_config.py    # Milvus配置
│   └── redis_config.py     # Redis配置
├── utils/                  # 工具函数
│   ├── __init__.py
│   ├── logger.py           # 日志工具
│   ├── exception_handler.py # 异常处理
│   └── decorators.py       # 装饰器
├── tests/                  # 测试模块
│   ├── __init__.py
│   ├── test_agent.py       # Agent测试
│   ├── test_rag.py         # RAG测试
│   └── test_api.py         # API测试
├── main.py                 # 应用入口
├── requirements.txt        # 依赖清单
└── Dockerfile              # 容器化部署
```

---

## 5. 目录功能详解

### 5.1 api/
- **v1/endpoints/chat.py**: 对话接口，处理用户与Agent的实时交互
- **v1/endpoints/investment.py**: 投资分析接口，提供商业地产投资分析服务
- **v1/endpoints/health.py**: 健康检查接口，监控服务状态
- **v1/schemas/request.py**: 请求参数模型定义（用户问题、会话ID等）
- **v1/schemas/response.py**: 响应数据模型定义（分析报告、建议等）
- **dependencies.py**: FastAPI依赖注入（Redis连接、Agent实例等）

### 5.2 agent/
- **main_agent.py**: 主Agent实现，负责意图识别、Agent选择、任务分发和结果聚合
- **market_agent.py**: 市场分析Agent，调取地产行情、政策、地块数据进行市场分析
- **revenue_agent.py**: 收益测算Agent，计算租金收益、回报周期、收益率
- **risk_agent.py**: 风险合规Agent，进行政策校验、风险提示、合规审查
- **tools/market_tools.py**: 市场分析相关工具（地产行情API、政策数据库、地块查询）
- **tools/revenue_tools.py**: 收益测算相关工具（租金测算、财务计算器、可行性分析）
- **tools/risk_tools.py**: 风险合规相关工具（合规审查引擎、风险评估模型）
- **tools/rag_tools.py**: RAG检索工具封装

### 5.3 rag/
- **vector_store.py**: Milvus向量库CRUD操作封装（插入、查询、更新、删除）
- **retriever.py**: 多路召回策略实现（ANN向量检索、BM25关键词匹配、语义相似度）
- **rrf_reranker.py**: RRF(Reciprocal Rank Fusion)融合排序算法实现
- **knowledge_base.py**: 知识库文档管理（文档解析、向量化处理、增量更新）

### 5.4 llm/
- **model.py**: 千问7B模型加载和推理封装
- **lora_adapter.py**: LoRA适配器集成，支持增量微调参数加载
- **prompt_template.py**: 商业地产领域专用提示词模板（市场分析、收益测算、风险合规）

### 5.5 session/
- **redis_client.py**: Redis客户端封装（连接池管理、基础操作）
- **session_manager.py**: 会话生命周期管理（创建、更新、销毁）
- **context_store.py**: 对话上下文序列化与存储（历史记录、中间状态）

### 5.6 config/
- **settings.py**: 全局配置（从环境变量读取，数据库连接、API端口等）
- **agent_config.py**: Agent行为参数配置（工具列表、调用策略、超时设置）
- **milvus_config.py**: Milvus连接和索引配置（地址、端口、索引类型、分片策略）
- **redis_config.py**: Redis连接和缓存策略配置（地址、端口、过期时间）

### 5.7 utils/
- **logger.py**: 结构化日志配置（日志级别、格式、输出位置）
- **exception_handler.py**: 全局异常捕获和处理（统一错误响应格式）
- **decorators.py**: 自定义装饰器（限流、日志记录、性能监控）

---

## 6. 关键技术指标

| 指标 | 优化前 | 优化后 | 提升幅度 |
|------|--------|--------|----------|
| 投资方案生成准确率 | 65% | 91% | +26% |
| LoRA微调后准确率 | 72% | 88% | +16% |
| 支持并发用户数 | - | 1000+ | - |
| 知识库规模 | - | 2万+商业地产文档 | - |
| 标注数据集规模 | - | 1万+金融/地产对话数据 | - |

---

## 7. 部署架构

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         生产环境部署架构                                │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────┐    ┌──────────┐    ┌──────────────────────┐              │
│  │  Nginx   │────│ Sentinel │────│     API Pods          │              │
│  │ (负载均衡)│    │ (熔断限流)│    │   FastAPI × 3节点    │              │
│  └──────────┘    └──────────┘    └──────────┬───────────┘              │
│                                              │                        │
│                        ┌─────────────────────┼─────────────────┐        │
│                        ▼                     ▼                 │        │
│              ┌──────────────┐      ┌───────────────────┐      │        │
│              │    Redis     │      │      Milvus       │      │        │
│              │  (会话缓存)   │      │   (向量数据库)     │      │        │
│              │  主从集群    │      │   分布式部署      │      │        │
│              └──────────────┘      └─────────┬─────────┘      │        │
│                                              │                │        │
│                                              ▼                │        │
│                                    ┌───────────────────┐      │        │
│                                    │      LLM服务       │      │        │
│                                    │   千问7B+LoRA     │      │        │
│                                    │   独立推理节点    │      │        │
│                                    └───────────────────┘      │        │
│                                                              │        │
└──────────────────────────────────────────────────────────────────────────┘
```

**部署说明：**
- **Nginx**: 反向代理和负载均衡，配置健康检查，支持SSL/TLS终止
- **Sentinel**: 熔断限流保护，支持流量控制、熔断降级、热点隔离
- **FastAPI**: 容器化部署，3节点水平扩展，支持自动扩缩容
- **Redis**: 主从集群模式，保障会话数据高可用
- **Milvus**: 分布式部署，支持向量数据分片存储
- **LLM服务**: 独立推理节点，支持模型热更新和动态批处理
